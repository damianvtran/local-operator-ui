/**
 * Retired in PR169 round 2. Historical frames/readbacks name this instrument;
 * its original implementation remains in git at 85eb7546a, not as an executable
 * agent workflow. No raw-CDP or private-browser exemption was ever authorized.
 * Use the browser-tool runbook in docs/evidence/draft-splash/README.md instead.
 */
throw new Error(
	"Draft-splash raw-CDP capture is retired. Use the browser tool and the evidence README runbook; if browser access is unavailable, record BLOCKED.",
);
