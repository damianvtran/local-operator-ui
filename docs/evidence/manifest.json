{
  "head": "927ad278948e6bfaf05a6de420085a10773ffaa4",
  "srcTree": "8af0e67395da05024f437135eca7beee85c82d1f",
  "scriptsTree": "4726627d1a5e4c3882b826c9ade5c6253877724f",
  "dirtyWorkingTree": false,
  "capturedAt": "2026-08-06T18:36:03.218Z",
  "frames": 594,
  "surfaces": 95,
  "themes": 12,
  "countsMean": {
    "frames": "The sweep's own frame count, asserted by `check-evidence.mjs` against the frames on disk that fall outside any declared `supplementary` set - 740 at this head, and the assertion is the definition rather than a restatement of it: 870 `.webp` are committed and the 18 `supplementary` sets below account for 130 of them (one of those sets declares no frames), leaving 740. This branch's contribution to those two terms is the sixteen `clear-search/` frames - a live-app set it declares below, so the SWEPT term is unmoved; it ran no sweep, so `head`, `capturedAt` and `dirtyWorkingTree` above are carried over from that run and still describe it, but `srcTree` and `scriptsTree` are not: this PR re-stamped them from the sweep's carried pair to its own (`1f3f94fc` to `8af0e673` for `src`, `b94c6290` to `4726627d` for `scripts`); both values move with the base - the `scripts` pair moved again when this branch was rebased onto `#138`, which edited `scripts/` - so a reader should compare them against `git rev-parse HEAD:src HEAD:scripts`, which is the equality `check-evidence.mjs` asserts, rather than against the literals here, which `check-evidence.mjs` requires of any head because that pair is the thing that tells a reader whether the committed frames are pictures of the current source. `head` and `srcTree` here therefore name two different trees by design - the sweep's and this branch's - and a reader reconciling the stamps should read them that way rather than as one capture record. Re-derived for THIS head and stated once. Earlier revisions of this field appended a fresh derivation per merge and left the previous ones standing as though they still described the tree, so it accumulated half a dozen figures from superseded heads and, after the 0.19.5 rebase, a subtraction that no longer resolved - the sibling terms were updated and the remainder was not. Those figures are deleted rather than quoted here: a reader scanning this field for a number should not find a wrong one under any framing. A stale derivation is worse here than no derivation, because this field's entire justification is that a reader can re-compute it: one that does not resolve teaches them to stop checking. The three terms above are the whole arithmetic, they are read from the tree rather than transcribed (`find docs/evidence -name '*.webp' | wc -l`, and the summed `frames` of the sets below), and `check-evidence.mjs` fails when they disagree with it - which is what makes the sentence checkable rather than merely asserted. This branch carries no version bump of its own: it is rebased rather than merged, so `package.json` on it reads whatever the base it was rebased onto carried, and a release landing on `main` afterwards only moves the number under a branch that never set it. On this head `package.json` is `0.19.7`, and its rebase base carries the same `0.19.7` - the base is deliberately not named, because every rebase moves it and a literal here has already been wrong twice; read `git merge-base HEAD origin/main` for the current one. The value is the base's, so the claim is \"no bump against its base\" rather than a number tied to any one head or rebase.",
    "surfaces": "`capture-evidence.mjs`'s `STORIES.length` at the head above - 108 entries, counted off the merged literal rather than summed from the two sides' fields (96 on this branch before the rebase and 107 on `main`, each written by a run taken before the other side landed - which is exactly why the merged figure is counted and not added). `main` brought the model-picker surfaces (11 `chat-model-picker--*` states plus the `chat-session-status-strip--model-switch-pending` band); this branch adds the one story it registered, `chat-tool-rows--trace-gap-boundaries`, and 107 + 1 = 108 is the reconciliation. `check-evidence.mjs` counts the list and fails a mismatch (round 4, R3), so this field cannot silently lag its own source.",
    "themes": "`capture-evidence.mjs`'s `THEMES.length` - 12."
  },
  "partialCapture": {
    "note": "Neither the 456-frame sweep nor the 12-theme sweep was regenerated. Frames added or refreshed here were captured against the same guards (assertFramePaints) but only in the two brand themes, and the live-app frames came from the real Electron window rather than Storybook. The spacing round REFRESHED the five chat-tool-rows surfaces because the row pitch changed under them, and added two: `spacing-uniformity` reproduces the three runs the operator screenshotted, `turn-boundary` shows the hierarchy that survives the tightening. `narrow` and `working-labels` are now captured in a viewport sized to their content — at 1280x900 they were mostly empty ground and crossed the uniformity ceiling once the rows tightened. REMEDIATION ROUND 1: all seven chat-tool-rows surfaces were refreshed again, because the name/summary gutter, the running row's live clock, the sr-only outcome text and the summary fallback all changed what they show. REMEDIATION ROUND 2: `real-conversation-tool-rows` was RE-CAPTURED and is no longer stale. It now shows 38 real tool rows with 0 blank object columns on a 20px pitch, rendered by the shipped reducer and the shipped CanonicalTranscript over real /history pages paged oldest-first in 20-entry pages (the cross-page condition the blank-summary defect needed). Electron is not required for this surface — the desktop API answers a plain bearer request. The reading was proven falsifiable by running the SAME harness with the reducer starved: stripping tool_calls alone still yields 0 blanks (the output fallback carries it), stripping tool_calls AND output yields 53 of 53 blank, reproducing the original defect from the current head. DIFF BODY: two surfaces were ADDED — the `write`/`edit` row's expansion now paints the result's own unified diff instead of its arguments JSON — and four frames were taken for them in the two brand palettes (`chat-tool-rows--diff-body` at 1280x2110 and `chat-tool-rows--diff-body-narrow` at 560x1380). The four Storybook frames are committed under `write-edit-diff/` rather than under the story-id directory the capturer derives (`chat-tool-rows/<leaf>`), following the `tool-rows/` precedent, so that the frames sit beside the spec and README that explain them; a re-capture writes them back to `chat-tool-rows/`. `surfaces` counts the two new STORIES entries. The 12-theme sweep was NOT regenerated. A sixth frame set is declared in `supplementary` below: three real durable diff rows in one conversation (197, 58 and 14 diff lines — the first two longer than the 40-line display cap, so two overflow markers and one body shown whole) rendered by the SHIPPED reducer and transcript over real transcript.jsonl rows. The story proves the component; that one proves the wire payload reaches pixels. DIFF BODY, REMEDIATION ROUND 1: the same two Storybook surfaces were refreshed again and a THIRD was added, because the body now paints each line whole in its kind's ink instead of tinting only the leading marker (the terminal's loop is the authority; its docstring promises the other thing), and because the cap's `… N more diff lines` marker is now pinned to the well's foot with `sticky -bottom-3 pb-3 -mb-3` — it used to scroll out of view in exactly the wrapped-at-cap case it exists for. The new surface, `chat-tool-rows--diff-body-narrow-wrapped-cap` at 560x830, is the frame that shows the marker visible at rest; `frames` moves 478 -> 480 for its two palettes, and it is the only frame count that changed. The `real-durable-diff-rows` pair was RE-CAPTURED in the same round for the same reason (on the same real conversation, same three diffs of 197, 58 and 14 lines) and carries the identical marker arithmetic. It was captured from the committed source, not from a dirty tree: this partial run's `head`, `srcTree` and `scriptsTree` name the commit whose pixels these are. PROVENANCE OF THAT NAME, AS OF THE THIRD REBASE HOP: that round's frames were captured at `601a7eece`, a rebase finish commit that was amended into `a5e20c4bc`, so the name recorded here was a commit the shutter had not yet produced. Both SHAs were discarded by the third hop onto 4455c5705 and are reachable from no branch. The `chat-usage` frames have since been re-captured at the current head `69664578f`, which is the commit `head`, `srcTree` and `scriptsTree` above now name and which IS reachable, so the capture no longer predates the SHA it is attributed to and no reader has to take a discarded commit on trust. The neighbour `tool-rows/*` frames were NOT re-captured — they are from the August head and do not reproduce pixel-exactly on this one (a uniform 11px shift with identical internal geometry, across three stories of different content), which the write-edit-diff README records so nobody reads it as a regression from this change. USAGE REVIEW ROUND 1: all twelve `chat-usage` story surfaces were RE-CAPTURED in both brand themes (24 frames) because the round changed what they show, not merely how they look. Two of them were the point: `loading` and `fetching` previously photographed states the shipped container could not produce (an ENABLED `Ask providers now` at first paint, when react-query reports isLoading and isFetching together; and cached numbers retained during a live ask, which the query key switch discarded). Both states are now reachable in the app, so both frames are captured from the real behaviour rather than from hand-fed props, and the agreement between story args and container props is pinned by scripts/usage-container.test.mjs rather than by this note. No story was deleted, so `frames` (498) and `surfaces` (57) are unchanged and still equal what is on disk. The other ten were re-captured because the fixes are visible in them: the bar track now carries a `border-control` boundary (measured 1.11:1 -> 2.05:1 against the card on the rendered dark frame, which is what makes a 0% row read as an empty meter and a 100% row as a full one), the unmeasurable window's dotted rule moved off floor-exempt `ink-disabled` onto `ink-dim`, staleness is measured against the set's newest confirmation so `stale-report` again shows one marked block beside one unmarked, prose left monospace, and the tally and the dead-grant note changed wording. `chat-usage--loading`'s capture viewport grew 360 -> 470 so the third skeleton block is not sliced through its own card border. `real-data/` was ALSO re-captured, from the same real backend response replayed through scripts/usage-real-evidence.mjs --payload, because it is the frame two of the fixes were diagnosed from: it read `Cached report, just now.` above blocks dated `1h ago` and showed all eleven blocks degraded. It now reads `Cached report, 34m ago.` and marks 10 of 11 - openrouter, the one account genuinely confirmed at the newest stamp, is unmarked - so the staleness mark distinguishes something again. USAGE REVIEW ROUND 2 (remediation): the same twelve `chat-usage` story surfaces were RE-CAPTURED in both brand themes (24 frames) because the scroll-fold treatment changed what most of them show. The fold rule is no longer an unconditional per-picker prop: it is measured per state from the scroll container, so the six states whose content ends above the fold (`empty`, `query-error`, `fetching`, `percent-only`, `remaining-balance`, `narrow`) no longer draw a boundary under nothing, and the states that do overflow carry a `border-control` rule (3:1 structural floor, pinned at the call site in scripts/contrast-contract.mjs) plus a 20px bottom fade that retracts at the end of the list. `loading` also changed copy: the first paint reads `Reading cached usage` rather than `Asking providers`, because opening the view is a cache read and not a provider ask. No story was added or deleted, so `frames` (498) and `surfaces` (57) are unchanged and still equal what is on disk. `real-data/` was NOT re-captured this round and is STALE with respect to the fold treatment: it still shows the round-1 clip. This machine's backend currently returns zero provider reports, so scripts/usage-real-evidence.mjs cannot re-derive the frame, and no saved `--payload` response from the 11-report run survives in the repository (it is deliberately never committed). The fold fix was instead verified at that exact density in the interaction harness - 11 provider blocks, 22 windows, 1217px of content in a 520px box - measuring a 1px `border-control` rule and a gradient mask at the top of the scroll and the mask retracted with the rule still present at the bottom, in both brand themes. Everything else `real-data` is evidence for (the staleness baseline, the description line, the multi-account keying) is unaffected by this round's changes. USAGE REVIEW ROUND 3: the story `chat-usage--dense` was ADDED (the thirteenth chat-usage surface, 2 frames in the two brand themes), so `frames` (498 -> 500) and `surfaces` (57 -> 58) were corrected by hand per the accounting rule above. It exists because the only committed frame at overflowing density was `real-data/`, which is one commit stale against the fold treatment and cannot be re-derived on demand; `dense` is the same density built from fixtures - eleven reports, twenty-eight windows, five anthropic accounts, the identities and labels the real response shapes - so any sweep can re-take it. The fold it photographs was measured in the same session against the same Storybook build, in both brand themes: 1273px of content in the 520px body cap (753px hidden), a 20px gradient mask plus a 1px `border-control` rule on the wrapper at scrollTop 0, the mask RETRACTED with the rule still present at scrollTop max, and no treatment at all on a fitting state (`percent-only` measured 0px hidden, no mask, 0px border). `real-data/` was NOT re-captured, and this time the live refill was attempted for real: the exact request the button makes (GET /v1/desktop/usage?live=true&refresh=true, authenticated with the running server's own desktop bearer) answered source=live with ZERO reports in ~50ms, and explicit per-provider asks for every usage-capable provider also returned zero - the backend's current credential view has no usage-reportable providers, so its cache cannot be refilled from here. `real-data/` therefore remains stale with respect to the fold treatment, its staleness stays recorded, and the fold at real density is evidenced by `dense`, which is re-derivable. REBASE ONTO main, THREE HOPS: this branch was cut from 2217ea59a and rebased first onto #114's head (104b0321a, the write/edit diff body), then onto 1f241ba87 (#111's backend-composed toasts plus the 0.17.1 release bump), and finally onto 4455c5705 (#107's update-installer safety work plus the 0.17.2 release bump) - each time because main moved while the gates were running. The files both sides touch were MERGED rather than taken from one side; on the third hop the overlap was again exactly `docs/evidence/manifest.json`, `scripts/capture-evidence.mjs` and `package.json`, and every file #107 owns (src/main/update-*, src/preload/*, the update-* and check-for-updates-button components, scripts/update-robustness.test.mjs, scripts/notarize*, scripts/build-env.mjs, scripts/verify-macos-artifacts.mjs, .github/workflows/publish.yml, docs/CODE_SIGNING.md and its two evidence sets) arrived from main byte-identical - verified by comparing all 41 blob SHAs against 4455c5705. `test:desktop` is now the union of four branches' lists (16 files, #107's update-robustness suite included, so no PR's regression cover was dropped) and the version stays at main's 0.17.2 with no bump on this branch. `surfaces` is the union of every STORIES set (66 = this branch's 61 plus #107's five new `common-updatenotification` entries) and `frames` is recomputed from the tree rather than carried from either side (518 outside the three declared sets; 544 files committed, the difference being 22 `sidebar-new-chat` + 2 `write-edit-diff/real-durable-diff-rows` + 2 `chat-usage/real-data`). The thirteen `chat-usage` story surfaces were RE-CAPTURED at this head (26 frames, both brand themes) because `capture-evidence.mjs` itself changed in the merge and #107 changed `src/main` and `src/preload`, so `head`, `srcTree` and `scriptsTree` above name the commit these frames actually came from and it is reachable on this branch. All 26 reproduced BYTE-IDENTICALLY to the pre-rebase set the review, QA, design and UX rounds signed off - same SHA-256 for every file - which is the measured evidence that none of #114, #111 or #107 changed anything this view renders and that the design sign-off still describes the pixels on the current head. The capture ran with `--allow-backend` because a backend belonging to another session was listening on the configured port; that flag is sound here and only here because every `chat-usage` story renders `UsageDialog`, which by construction does no fetching (the query lives in `UsageView`, which no story renders), and the byte-identical result is itself the proof that no live reply reached a frame. The two `chat-usage/real-data` frames were NOT re-captured and are the same bytes as before, so their recorded staleness against the round-2 fold treatment is unchanged and still accurate - a rebase does not make a frame fresher. #107's, #114's and the older `tui-parity/` frames are byte-identical to main: the `--only=chat-usage` runs wrote nothing outside `chat-usage/`. So the frames in this tree come from four heads, not one: `chat-usage/` story frames from this head 69664578f, `common-updatenotification/` and `update-robustness/` from #107's own capture, `write-edit-diff/` from #114's own capture at 96171dac, and the older sweep frames from the August head as recorded above.\n\nTHIS BRANCH (fix/tool-row-args-recovery) ON TOP OF THAT: the tool row's object column is recovered from a mid-turn snapshot's seed, which carries settling frames and no arguments — so rows that read `exit code: 0`, `Edited <path>`, `result: None` or a bare line number now name the call. The eight `chat-tool-rows--*` surfaces were refreshed in the two brand themes, `states` gained a composing row and `joined-mid-turn` a silent call and a truncated stand-in row, and every line that reaches the object column from the RESULT rather than from the call now carries a leading `…` so a column that is normally a command cannot be misread as one. A sweep also no longer deletes non-frame files: it clears the `.webp` frames it can retake and keeps the READMEs, the hand-taken PNG pairs and `tui-parity/OPERATOR-TUI-REFERENCE.md` — 123 committed files that a sweep used to take with the frames, with this gate green over the loss. Six sets are now declared: the two above, this branch's `tool-rows-baseline` (the before/after pair, which needs an older source tree), `chat-tool-rows/spacing-uniformity`, `chat-tool-rows/real-conversation-tool-rows`, `transcript-images/real-conversation-images` and `onboarding-onboardingmodal/radient-sign-in` (its story was removed with the Tailwind v4 landing, and the dead STORIES entry made every full sweep abort before this fix). `docs/evidence/tool-rows` was renamed to `chat-tool-rows` because the capturer derives a surface directory from the story title slug, and the title gained a `Chat/` prefix after the set was first written.\n\nMERGE UP TO `origin/main` (#114, #113, #111, #107): the resolution re-captured, at the merged head, the eight `chat-tool-rows--*` story surfaces, the six `chat-trace--*` ones and the three `write-edit-diff/diff-body*` ones (two byte-identical to main's; the wide pair replaced in place because this branch's row spellings changed it, and the derived copies under `chat-tool-rows/` deleted rather than left beside them). Three of the `chat-trace` surfaces came back at 900px on the first attempt, because `capture-evidence` sizes a frame with `Math.max(documentElement.scrollHeight, declared)` and the transcript has been its own scroll container since #101: the document then reports the viewport, the max collapses to the declared 900 and the frame clips the conversation - `conversation-reasoning-open` lost 2,427px including two of the three open reasoning panels it exists to evidence. They now declare their content heights (1308 / 1409 / 3327) and were re-captured at them, which is what `refreshedStories` above describes.\n\nTHEN A SECOND MERGE UP, on to #110 (the provider usage view): that commit added `chat-usage/*` frames and its own `chat-usage/real-data` declaration, both carried here; the union of the two manifests is what this file holds. REMEDIATION ROUND: the eighteen `chat-run-details` frames were re-taken at `48d1357ab` (Storybook on 6017, `--only=run-details --themes=localOperatorDark,localOperatorLight --allow-backend`), the first capture this branch has taken since it was merged with `origin/main`: `dirtyWorkingTree` is false and the two tree hashes name that commit. All eighteen moved, where the previous round had thirteen byte-identical - the story now paints `canvas` under the popover instead of `surface` (#113), so a ground pixel that read `(30,26,21)` dark reads `(23,19,14)` now. At zero fuzz that is 29-85% of each frame's pixels, which is a 7-unit channel step and not damage; the two causes are separable at stated thresholds, because an 8% fuzz is a 20-unit channel tolerance and larger than that step. At 8% the only frames with any differing pixel at all are the ones whose CONTENT changed - `crowded` (870 dark / 838 light) and `subagents-only` (3801 / 3979), whose rows the slice now selects by the children's own clocks, plus `both-in-flight` light (83) for its live clock. The panel's own separation was re-measured on the new ground: dark L* 6.14 -> 13.62 and light 95.28 -> 99.43, against the 4.41 / 1.78 step the previous set certified over `surface`. Two of the fixtures changed shape with the code: `crowded` is now stored deliberately out of chronological order, so the frame is a guard for the tie-break rather than a picture that agrees with every rule, and it no longer carries a `paused` row - a state this wire cannot produce. `frames` (516) and `surfaces` (75) are unchanged by the round: no frame was added or removed.\n\nINTEGRATION ONTO #109 (`ef8719a44`): this merge united main's story list with this branch's, and this file was reconciled against the merged tree rather than against either side's scalar - the two sides' `frames`/`surfaces` each described a sweep taken before the other side landed, so neither describes the union (see `countsMean`). The gate's reason for a re-capture is that a stamp is only honest when the tree hashes match the tree under review, and the merged `src`/`scripts` are not the ones the eighteen `chat-run-details` frames were taken from - so they were re-taken at `ae5e615b4` (Storybook 6017, `--only=run-details --themes=localOperatorDark,localOperatorLight --allow-backend`; `dirtyWorkingTree` false and both tree hashes naming that commit). `head` names that commit rather than an older ancestor because the merge commit was re-committed with the branch's conventional message BEFORE the capture: amending a stamped commit is what orphans a stamp, so the re-stamp follows the rewrite instead of the other way round. Seventeen of the eighteen frames came back BYTE-IDENTICAL, which is the measurement that this branch's surfaces render the same way after #109's renderer changes; only `both-in-flight/localOperatorDark.webp` moved, and its differing pixels (14,240 at zero fuzz - 4.89% of the frame - and 241 at 2%) are confined to the two running rows' `19s` -> `18s` and `1m13s` -> `1m12s` elapsed labels, i.e. the story's live clock and not a layout or colour change. No frame was added or removed: `addedFrames` 0 and `addedSurfaces` empty, so `frames` stays 544 and `surfaces` stays 90.",
    "addedFrames": 0,
    "addedAt": "2026-09-13T06:50:46.318Z",
    "addedAtHead": "7a88e7a73a7bde87032a0f93c520bb4d491fc5c0",
    "addedSurfaces": [],
    "refreshedAt": "2026-09-13T06:50:46.317Z",
    "refreshedAtHead": "7a88e7a73a7bde87032a0f93c520bb4d491fc5c0",
    "refreshedFrames": 12,
    "refreshedStories": [
      "canvas-workspace--files"
    ],
    "refreshedThemes": [
      "localOperatorDark",
      "localOperatorLight",
      "dracula",
      "dune",
      "sage",
      "monokai",
      "tokyoNight",
      "iceberg",
      "radient",
      "neon",
      "obsidian",
      "synth"
    ],
    "addedBySweptRefresh": 0,
    "addedDirectories": [],
    "provenanceNote": "Re-captured at this head after the rebase onto main: the strip's 20 brand-theme frames came back BYTE-IDENTICAL to the pre-rebase ones, which is the evidence that uniting the two series changed no rendering of this branch's own surfaces. A full sweep is not possible on any current tree - `shell-app-shell--settings-appearance` times out preparing in Storybook, reproduced on clean origin/main, so it is pre-existing rather than this branch's. `fonts.css` gains `font-display: swap`, which changes what renders WHILE a face loads and not the settled paint: re-capturing `chat-tool-rows` (the most monospace-dense surface) moved 7 of 22 frames by 2-16 bytes of ~15kB, i.e. encoder noise and no pixel a reviewer can see, so main's frames are left as committed rather than churned. Re-stamped for the #116 sync merge: that commit changes `src/main/` and two scripts, so both tree hashes move, but it touches NO renderer source (`git diff --name-only 57ff92432 HEAD -- 'src/renderer/**'` is empty), so every frame is still a picture of the code under review and none were re-captured. `head` is unchanged - the provenance ancestor stays reachable and an ancestor across an unrelated merge. Re-stamped again for the main sync merge (#106 + 0.17.3): the trees move whenever anything under src/ or scripts/ moves. #106 changes renderer files, but the intersection of its `src/renderer/**` changes with this branch's is EMPTY, so its frames and ours each still depict their own code and neither set is stale - no re-capture. RE-STAMPED FOR THE ROUND-3 FIXES: the ten stories' twenty frames were re-taken over the round-3 remediation content while it was still UNCOMMITTED, so the capture's own stamp named the pre-commit HEAD - `head`, `refreshedAtHead` and `addedAtHead` are re-pointed at the commit that records exactly that content (`2a03051a7`), and `dirtyWorkingTree` is false because that content is committed rather than dirty. The trees are the ones the pixels came from: this re-stamp commit touches `docs/evidence/manifest.json` alone, so nothing under `src/` or `scripts/` moved between the capture and the stamp, and the gate's own comparison against `HEAD:src`/`HEAD:scripts` is what confirms it. The two added frames are `chat-run-details/restored-and-unrecognised/` (the tenth story), and `surfaces` 91 is the declared `STORIES` count at this head with the new entry, counted off the literal rather than incremented. RE-STAMPED FOR THE #112 SYNC MERGE (#112 plus the 0.18.0 release): the merge moves `src/` and `scripts/`, so both tree hashes move with it and `head` moves to the merge commit `83db293ed`, the commit that holds these frames' own source. That is a measurement, not an assumption: the run-details stories' import graph was walked at the merged head - 136 modules, from `run-details.stories.tsx` through `ChatHeader`, `MessageItem` and `TraceGroup` - and reaches NONE of #112's changed renderer modules (`canonical-transcript.tsx`, `older-history-slot.tsx`, `use-scroll-paging.ts`, `scroll-paging.ts`, `use-canonical-session.ts`), so nothing was re-captured and no frame here is stale. The round-4 fixes could not have moved a pixel either: R4-1 changes only the word the fold REFUSED while the sanitiser did not, and no fixture carries a recognised state plus junk, so `restored-and-unrecognised`'s pair - the only frames the new rule can reach - are unchanged. `frames` 594 and `surfaces` 95 are the merge's own arithmetic (546 + main's 48 swept `chat-older-history-slot` frames; 91 + main's four boards) and both are the gate's own terms rather than carried-over fields: `check-evidence.mjs` compares the first against the `.webp` outside the declared sets and the second against the merged `STORIES` literal. This commit touches `docs/` alone, so the trees recorded here are still the ones the pixels came from. Re-stamped for the rebase onto #120 (79d0dc889): this branch moves eight files under `src/` and five under `scripts/`, so both trees move, and `git diff --name-only 79d0dc889 HEAD -- 'src/renderer/**'` is empty with no `scripts/capture-*` file changed - every committed frame is still a picture of the code under review, so nothing was re-captured and this is a stamp. The ids are read at this head rather than carried over from either earlier stamp, and #120's own stamp is kept as this note's earlier sentences describe it. `head` is untouched and still reachable, and the stamp lands as a separate trailing commit rather than an amend, because the field is self-referential. CANVAS RE-CAPTURE (mentioned-files viewers): the Files grid's renderer changed, so the twelve `canvas-workspace/files` frames were re-captured across all twelve themes at this head. The sweep was first run narrowed to the whole canvas surface (`--only=canvas-workspace`), and eight of its nine stories came back PRE-SETTLED on a host at load average ~260: `variables/localOperatorDark` reads \"Loading variables\" and `code-focused/radient` shows the fallback face mid font-swap. Those 96 frames were therefore restored from the previous commit and only `files` was refreshed, which is exactly what `refreshedStories`/`refreshedFrames` record. `frames` 594 and `surfaces` 95 are unmoved - the refresh added no surface and no frame, and the `.webp` total on disk is still 681 with the declared sets unchanged. `dirtyWorkingTree` is false because the only non-clean entries at capture time were the untracked design note for this change (never committed) and the evidence frames under production; `git status --porcelain -- src scripts` was empty at the capture head, which is what the field exists to assert, and the tree hashes above are the trees the pixels came from. DECLARED SET ADDED (mentioned-files-app): two live-app frames, the Files panel in the dark brand palette and the PDF viewer in the light one, captured against an ISOLATED backend on a scratch config dir rather than the operator's store on 1111, with every file and every transcript row generated by the harness - a whole-window frame of the operator's real session carries their session titles, transcript and documents, and this repository is public. They are a declared set rather than swept frames for the two reasons the sweep cannot cover: headless Chrome has no PDF viewer, and the panel's producer and thumbnails need a real backend and a real transcript. `frames` 594 is unmoved - the set is outside it by construction, which is what makes the total on disk 683 = 594 + 89 across fifteen declared sets.",
    "previousRefresh": {
      "refreshedAt": "2026-09-13T07:23:43.292Z",
      "refreshedAtHead": "32c7751583627a8c74e9250798b6525e6e1931a2",
      "refreshedFrames": 144,
      "addedFrames": 144,
      "addedSurfaces": [
        "chat-model-picker/populated",
        "chat-model-picker/hovered",
        "chat-model-picker/keyboard-highlight",
        "chat-model-picker/busy",
        "chat-model-picker/result",
        "chat-model-picker/persist-checked",
        "chat-model-picker/refresh-pending",
        "chat-model-picker/loading",
        "chat-model-picker/empty",
        "chat-model-picker/partial-error",
        "chat-model-picker/narrow",
        "chat-session-status-strip/model-switch-pending"
      ],
      "addedAt": "2026-09-13T03:30:00.000Z",
      "addedAtHead": "32c7751583627a8c74e9250798b6525e6e1931a2",
      "note": "The capture that ADDED this branch's twelve picker/band surfaces. Recorded here because the remediation round's own narrowed capture overwrites `addedFrames`/`addedSurfaces` in place, and an addition that disappears from the manifest reads as though the stories had always been swept."
    }
  },
  "supplementary": [
    {
      "path": "chat-title",
      "frames": 16,
      "surfaces": 8,
      "themes": 2,
      "source": "the REAL built Electron app: npx electron . from each tree's own out/ (built with pnpm build, so import.meta.env.DEV is false and the development-only Chat|Raw tab strip does not exist), against an isolated local-operator serve on its own port, seeded from out/evidence-harness/seed.mjs and paired through a shared LOCAL_OPERATOR_DESKTOP_TOKEN; driven over raw CDP by out/evidence-harness/app-drive.mjs. The before tree is a detached worktree at a09f2e6f4 with the same script and the same backend.",
      "why": "The claim is a CLICK that both opens a conversation and writes its title into the catalogue store the sidebar reads, and the two surfaces it moves between - the sidebar row and the chat header - have no story (the sidebar's absence from the sweep is recorded in docs/evidence/sidebar-new-chat), so no sweep can re-derive these frames and Storybook would have to fake both the store write and the press. Captured from the built app rather than electron-vite dev because in dev the development-only Chat|Raw tab strip paints over the header: the first attempt at this evidence produced byte-identical before/after frames with a correct DOM and the subject covered.",
      "capturedAt": "2026-09-12T23:37:32.000Z"
    },
    {
      "path": "sidebar-new-chat",
      "frames": 22,
      "surfaces": 11,
      "themes": 2,
      "source": "live renderer over CDP",
      "why": "The claims are a real pointer hover and a click that stages a draft. Storybook can force the revealed class but not produce the hover, and a staged draft is store state a fixture would have to fake, so these are captured from the running app against an isolated seeded backend instead. The sidebar has no story, so re-running the sweep cannot re-derive them; docs/evidence/sidebar-new-chat/README.md carries the re-capture procedure.",
      "capturedAt": "2026-09-10T06:00:00.000Z"
    },
    {
      "path": "write-edit-diff/real-durable-diff-rows",
      "frames": 2,
      "surfaces": 1,
      "themes": 2,
      "source": "headless Chrome over CDP, driven by `node scripts/diff-body-evidence.mjs`: the shipped `applyHistoryPage` reducer and the shipped `CanonicalTranscript` over a real durable history page read from one real session's `transcript.jsonl` in the local store (`--session` and `--anchor` are required arguments; the id is deliberately not committed)",
      "why": "The story renders a TranscriptRecord this repository built, so it proves the component. This set starts one layer earlier, at the bytes `/v1/desktop/sessions/{id}/history` serves — the stored rows, serialised verbatim by `read_transcript_page` — and runs them through the shipped reducer, which is what proves the payload reaches pixels: the durable row's own encoding, the extraction of `provider_payload.details.diff`, the identity gate and the args-drop rule. It cannot be re-derived by the sweep, and the payload itself is deliberately NOT committed: the harness writes it to a temp file and deletes it. Re-run with `node scripts/diff-body-evidence.mjs --session=<id> --anchor=<row id>` (see docs/evidence/write-edit-diff/README.md).",
      "capturedAt": "2026-09-12T09:20:00Z"
    },
    {
      "path": "chat-usage/real-data",
      "frames": 2,
      "surfaces": 1,
      "themes": 2,
      "source": "scripts/usage-real-evidence.mjs over the shipped UsageDialog",
      "why": "The only frames in this tree rendered against a REAL /v1/desktop/usage response rather than story fixtures: 11 provider reports and 28 windows as the local backend actually returned them, which is what proves the ported view renders the wire shape and not merely its own fixtures. Storybook cannot produce them (the payload needs the desktop bearer), so the sweep cannot re-derive these two frames. Account identities are redacted shape-preservingly by the capture script; every number, window, tier, unit and staleness value is the backend's own. docs/evidence/chat-usage/README.md carries the re-capture procedure. RE-CAPTURED for review round 1 from the same real response (replayed via `--payload`, since this machine's backend now returns zero reports and cannot re-derive it): the committed pair was the evidence for the staleness-baseline and description defects, so leaving it would have shipped a picture of the bug beside its fix. ROUND 3: a genuine live refill was attempted (live=true&refresh=true with the server's own bearer, plus explicit per-provider asks) and the backend returned zero reports for every provider - its credential view currently has no usage-reportable providers - so these two frames remain one commit stale with respect to the round-2 fold treatment. The fold at this density is evidenced by the re-derivable `chat-usage--dense` story instead.",
      "capturedAt": "2026-09-12T07:48:54.000Z"
    },
    {
      "path": "tool-rows-baseline",
      "frames": 2,
      "surfaces": 1,
      "themes": 2,
      "source": "scripts/capture-evidence.mjs against unmodified origin/main (2217ea59a), same story and fixture as chat-tool-rows/joined-mid-turn",
      "why": [
        "The fixed frames cannot show what was wrong, and this pair cannot be regenerated by a sweep: a sweep captures the CURRENT tree, while these need main's reducer under the same story. It is declared as its own set so clearSweptFrames preserves it and the sweep's own count stays honest. See its README.md for the one-line reproduction."
      ],
      "capturedAt": "2026-09-12T00:25:00.000Z"
    },
    {
      "path": "chat-tool-rows/spacing-uniformity",
      "frames": 2,
      "surfaces": 1,
      "themes": 2,
      "source": "the live app over CDP, driven by hand for the spacing round that preceded this change",
      "why": "The three runs the operator screenshotted as \"much too wide\" and \"not very uniform\", reproduced as a regression surface. It has no story, so a sweep cannot re-derive it, and a sweep would DELETE it: `clearSweptFrames` keeps only what a declaration names. Declared here for the same reason `sidebar-new-chat` is.",
      "capturedAt": "2026-09-10T02:37:03.000Z"
    },
    {
      "path": "chat-tool-rows/real-conversation-tool-rows",
      "frames": 2,
      "surfaces": 1,
      "themes": 2,
      "source": "a bespoke vite harness mounting the shipped reducer and CanonicalTranscript over real /history pages from a real local-operator serve backend",
      "why": "The shipped reducer and transcript over a REAL conversation, paged oldest-first in 20-entry pages - the cross-page condition the blank-summary defect needed. The harness was never committed, so this is the one surface no sweep can regenerate; its durations predate the `<0.1s` spelling, and that staleness is recorded here rather than hidden.",
      "capturedAt": "2026-09-10T02:37:03.000Z"
    },
    {
      "path": "onboarding-onboardingmodal/radient-sign-in",
      "frames": 12,
      "surfaces": 1,
      "themes": 12,
      "source": "scripts/capture-evidence.mjs in the twelve-theme sweep before the story was removed",
      "why": "Historical: the Radient sign-in onboarding screen as it stood before the Tailwind v4 landing (bb57a4080) removed its story, which is what left the capture list naming an id that no longer exists. Kept as the record of the pre-v4 screen, and declared so a sweep neither deletes it silently nor reports it as unaccounted.",
      "capturedAt": "2026-08-06T18:36:03.218Z"
    },
    {
      "path": "transcript-images/real-conversation-images",
      "frames": 2,
      "surfaces": 1,
      "themes": 2,
      "source": "a bespoke harness over a real conversation's history and attachment endpoints",
      "why": "Two frames of the same real conversation as `chat-tool-rows/real-conversation-tool-rows`, captured to show what the transcript does with the images a `browser` or `read` call returns: a durable row carries only a content-addressed digest and the bytes are fetched separately, which is the path this surface proves end to end. It has no story and its harness was not committed, so a sweep cannot re-derive it - and until it was declared, a sweep DELETED it while the gate stayed green, because the sweep rewrites its own frame count in the same pass.",
      "capturedAt": "2026-09-10T02:37:03.000Z"
    },
    {
      "path": "write-edit-diff/diff-body",
      "frames": 2,
      "surfaces": 1,
      "themes": 2,
      "source": "scripts/capture-evidence.mjs, the `chat-tool-rows--*` story that renders the shipped CanonicalTranscript",
      "why": "The wide diff-body frame of the `write`/`edit` row's expansion. The capturer derives a surface directory from the story id, which would be `chat-tool-rows/diff-body`; upstream filed these beside this README, the DIFF-BODY-SPEC and the real-conversation rows instead so the spec and its frames sit together. Declared here so a sweep's derived writes are counted as ADDED frames rather than landing inside a set this manifest already accounts for, which is the arithmetic `check-evidence` asserts.",
      "capturedAt": "2026-09-12T09:20:00.000Z"
    },
    {
      "path": "write-edit-diff/diff-body-narrow",
      "frames": 2,
      "surfaces": 1,
      "themes": 2,
      "source": "scripts/capture-evidence.mjs, the `chat-tool-rows--*` story that renders the shipped CanonicalTranscript",
      "why": "The same row's diff body at 560px, where the body wraps. The capturer derives a surface directory from the story id, which would be `chat-tool-rows/diff-body-narrow`; upstream filed these beside this README, the DIFF-BODY-SPEC and the real-conversation rows instead so the spec and its frames sit together. Declared here so a sweep's derived writes are counted as ADDED frames rather than landing inside a set this manifest already accounts for, which is the arithmetic `check-evidence` asserts.",
      "capturedAt": "2026-09-12T09:20:00.000Z"
    },
    {
      "path": "write-edit-diff/diff-body-narrow-wrapped-cap",
      "frames": 2,
      "surfaces": 1,
      "themes": 2,
      "source": "scripts/capture-evidence.mjs, the `chat-tool-rows--*` story that renders the shipped CanonicalTranscript",
      "why": "The diff body at the wrap ceiling. The capturer derives a surface directory from the story id, which would be `chat-tool-rows/diff-body-narrow-wrapped-cap`; upstream filed these beside this README, the DIFF-BODY-SPEC and the real-conversation rows instead so the spec and its frames sit together. Declared here so a sweep's derived writes are counted as ADDED frames rather than landing inside a set this manifest already accounts for, which is the arithmetic `check-evidence` asserts.",
      "capturedAt": "2026-09-12T09:20:00.000Z"
    },
    {
      "path": "session-status-live",
      "frames": 12,
      "surfaces": 11,
      "themes": 2,
      "source": "live renderer over CDP against an isolated backend",
      "why": "Live frames from the packaged Electron app over real IPC, which Storybook cannot produce: a cold snapshot, a reload, the backend-down error path, and round 3's aggregator route plus cold-owner effort states. One theme only (localOperatorDark) because these prove BEHAVIOUR, not colour; the theme matrix is the Storybook set's job.",
      "capturedAt": "2026-09-12T09:53:17.005018Z",
      "capturedAtHead": "af374b62531b0275c6c8e070e3e85d45cbf35a75"
    },
    {
      "path": "transcript-scroll-paging",
      "frames": 7,
      "surfaces": 7,
      "themes": 1,
      "source": "live renderer over CDP, driven with real wheel events against a seeded backend",
      "why": "The claims are about MOTION -- that a fling issues one page rather than one per event, and that the row under the reader holds its offset while a page lands. Neither is a property of a rendered tree, so Storybook cannot express them: they need real wheel events reaching the real scroller with a real backend answering sessions.history, and the artefact that carries the claim is the per-frame measurement JSON beside the frames rather than the frames themselves. Captured one theme deep because nothing under test is palette-dependent; the slot's own appearance is swept across all twelve by chat-older-history-slot. The before set was captured on origin/main (2217ea59a) by the QA pass and shows the click-only behaviour this change replaces. Regenerate with scripts/scroll-paging-evidence.mjs; docs/evidence/transcript-scroll-paging/README.md carries the exact seeding recipe and ports.",
      "capturedAt": "2026-09-12T00:00:00.000Z"
    },
    {
      "path": "mentioned-files-app",
      "frames": 2,
      "surfaces": 2,
      "themes": 2,
      "source": "the REAL built Electron app - pnpm build, then electron . from this tree's own out/ with LOCAL_OPERATOR_UI_WINDOW_MODE=headless, its own --user-data-dir and its own CDP port - against an ISOLATED `local-operator serve` on a scratch LOCAL_OPERATOR_CONFIG_DIR/HOME and a scratch cwd, paired through a shared LOCAL_OPERATOR_DESKTOP_TOKEN; driven over raw CDP by out/evidence-harness/drive-mentioned-files.mjs, with the session seeded by out/evidence-harness/seed-mentioned-files.mjs and every path it mentions generated by out/evidence-harness/make-fixtures.mjs. The harness is gitignored under out/, like the chat-title and sidebar-new-chat ones. Both frames are cropped to the canvas panel's own measured rect, so neither carries the sidebar, the session list or the transcript.",
      "why": "The two claims under review are claims about a running app that no sweep can photograph. The FILES PANEL is produced from a transcript at runtime - the panel's content is inferred from prose, tool args and tool output, and its thumbnails come from the backend's static route - so Storybook has neither the producer nor the files, and the sweep's own canvas-workspace--files frame says so by showing a fixture. The PDF VIEWER cannot be captured by scripts/capture-evidence.mjs at all: headless Chrome has no PDF viewer and renders a bare application/pdf document with no body (measured in the design note's Appendix A). Together they are the only evidence that a path an agent wrote becomes a tile, that a ghost path gets the `Not found` receipt, and that the PDF opens in Chromium's viewer under our own themed chrome bar with no CSP violation. They are captured against an ISOLATED backend rather than the operator's live one because a whole-window frame of a real session carries the operator's session titles, transcript and documents, and this repository is public.",
      "capturedAt": "2026-09-13T07:18:00.000Z",
      "capturedAtHead": "7a88e7a73a7bde87032a0f93c520bb4d491fc5c0"
    },
    {
      "path": "model-picker-feedback",
      "frames": 26,
      "surfaces": 16,
      "themes": 1,
      "capturedAtHead": "dc0e0073b8be7236028afb27d1180dd250abf7d9",
      "source": "the REAL built Electron app (`pnpm build`, then `docs/evidence/window-mode/harness/run.sh` with EXTRA_SEED=seed-model-picker.mjs and DRIVE_SCRIPT=drive-model-picker.mjs, headless, 1380x900) against an isolated `local-operator serve` on a port the script owns, 0 frontmost samples in every run and 0 renderer errors. Sixteen states, and they come from TWO runs because two starting states are needed: fifteen from the branch's own seed (no provider credential, i.e. the refusal path — the seed the `before-*` halves were taken under, so the pairs still differ by the change and not by the setup), and the three `needs-sign-in-*` frames from a second run on QA's credentialed seed (`/tmp/qa130/seed-qa.mjs`, a synthetic `openai` credential written through the backend's own AuthStore) — without a credential the owner refuses every switch, so a switch it ACCEPTS does not exist there. The second run's frames are prefixed `cred-` by the driver so no frame can be mistaken for the other seed's. `localOperatorDark` is the app's own default theme, which is the only theme the harness boots into. **U7 round.** The one frame UX U7 invalidated was re-captured on the fixed head and one `before-*` half was added beside it: `needs-sign-in-pick-settled` now shows `This session runs anthropic/claude-opus-5.` (the row the check is on) where the pre-fix frame showed `openai/gpt-4.1`, and that pre-fix frame is committed as `before-header-names-the-switch` — the only `before-*` half taken on this branch's own pre-fix head (`ccbeb0e2a`) rather than on the merge-base, because UX filed U7 against the branch rather than against the base. That run is also the first whose cache lives inside the scratch root: `run.sh` now sets `HOME=\"$SCRATCH/home\"` (QA round 2, Q3 — the catalogue cache resolves from `HOME`, not from the config dir), so the seed copies the same `*.listing.json` files the earlier run read into the scratch home, and nothing outside `/tmp` is written. Both runs' measurements are committed beside the frames (`picker-metrics.json`, `picker-metrics-credentialed.json`), so every number the README quotes is re-derivable from the tree.",
      "why": "the claim is that a hover, a keyboard highlight, an in-flight pick and the OUTCOME of that pick are perceivable — properties of computed style and of timing that no palette assertion and no component test can see. The unit suite asserts the classes, the transitions and the reading rule (`scripts/picker-feedback.test.mjs`); only a frame of the running app shows the ground and the edge the row actually paints inside a dialog whose own ground is `elevated`, and what the user is left with when the answer arrives after the dialog is gone. The `before-*` halves carry the defect as a measurement: the active row's computed background is `rgb(40,35,24)`, identical to the dialog's, and `before-pointer-left-list` is byte-identical to `before-keyboard-highlight` (AE 0 of 4,813,440 px). Seven of the sixteen states are after-only, and that is the finding rather than an omission: each is a state the old code could not produce or could not report — the pointer's structural edge in a palette where the wash collapses (D12), the footer naming the row Enter acts on while that row is off screen (U1), the band holding a labelled spinner while the pick is unconfirmed (U3), the refusal landing with the dialog already closed (U2), and the three `needs-sign-in-*` frames (Q1). The transcript note count is measured either side of the U2 case — 1 before the click, 2 after it, with the dialog closed 80 ms in — because the refusal sentence is identical for every refused pick and only a count can prove a second one was written. The U7 pair is measured rather than described: between the two frames AE is 85,307 px (1.77%) of 4,813,440, of which 13,230 px (2% fuzz) sit in one 952x64 band across the header sentence's own text line and 274 px are the clock.",
      "capturedAt": "2026-09-13T11:16:00Z"
    },
    {
      "path": "tool-rows-4px-rejected",
      "frames": 1,
      "capturedAt": "2026-09-13",
      "capturedAtHead": "d90ad6df7108e5e35eb7a18d336ebca244a9f5a2",
      "source": "scripts/capture-evidence.mjs against chat-tool-rows--operator-spacing-cases with GAP.trace temporarily set to mt-1, then reverted",
      "why": "The REJECTED alternative, photographed. Both the code comments and this directory's README rest the 'not 4px' half of the decision on 'at 4px the rows float as separate items again' - and no frame in the repository was at 4px, so the comparison that decides the tier is 2 rather than 4 could not be checked by looking, which is the standard this evidence set holds everything else to. A sweep cannot re-derive it: it is a picture of a tier the app does not ship, produced by editing GAP.trace to mt-1 for one capture and reverting. Compare against chat-tool-rows/operator-spacing-cases/localOperatorDark.webp, same story and viewport, which is the shipped 2px."
    },
    {
      "path": "submit-latency",
      "frames": 0,
      "surfaces": 0,
      "themes": 0,
      "source": "PENDING - no frame has been captured, and this entry exists so that is a declared state rather than an absence. The procedure that will produce them (the shipped `ChatPage` served to the operator's own Chrome through the dev desktop proxy, driven by a browser tool, against an isolated `local-operator serve` at `hosting: test`) is written out as a hand capture in docs/evidence/submit-latency/README.md, together with the three measurements of the bridge state that blocked it on 2026-09-13 and the filenames this directory is expected to hold.",
      "why": "Declared at zero frames rather than left out, because an undeclared directory is invisible to check-evidence while a declared one is a term the gate can fail on: the moment a `.webp` lands in this directory, `onDisk !== set.frames` and the gate names this set. That is the whole cost of declaring it - a green run here still says nothing about whether these frames are good, only that there are none. The `head`/`srcTree`/`scriptsTree` stamp above is deliberately NOT re-stamped for this entry: the stamp records a capture, and there has not been one (the same reason the two structural tree failures already on this branch stay visible instead of being papered over by a stamp that no capture justifies).",
      "capturedAt": "PENDING (declaration written 2026-09-13; see docs/evidence/submit-latency/README.md)"
    },
    {
      "path": "clear-search",
      "frames": 16,
      "surfaces": 8,
      "themes": 2,
      "source": "the live renderer over CDP - the app's own src/renderer/index.html served by Vite with the renderer aliases, Tailwind pipeline and desktopProxyPlugin, against an isolated local-operator serve backend on its own port with its own config dir, seeded with 7 agents, 2 teams and 7 sessions. Driven by `out/evidence-harness/capture.mjs` (gitignored, alongside the seeder and the Vite config): private headless Chrome, fresh user-data-dir, focus emulation on, and every wait on a condition rather than a duration. Re-running the `after` pass reproduced the committed frames byte for byte.",
      "why": "The claims are a real pointer click that changes React state and a caret that has to come back to a field, and Storybook can produce neither: the Chats sidebar has no story at all, so the sweep's STORIES list holds nothing that renders it, and a fixture could not produce the focus outcome of unmounting the button that was clicked - the thing this change had to get right. The `before` half also records the behaviour being replaced: Escape clears the query and BLURS, which is why three frames in the set are byte-identical by construction (rest on both trees, and the pre-change clear, which is indistinguishable from a field that was never filtered). A fifth state, `after-focused`, holds design round 1's D1: the control reached by keyboard, whose ring must sit inside the field rather than across its border (the shared `icon-sm` step's 1px offset needs 3px of clearance and a 28px control in a 32px field has 2px). docs/evidence/clear-search/README.md carries the readback that separates the states, the geometry in both palettes, and the re-capture procedure.",
      "capturedAt": "2026-09-13T16:35:00Z"
    }
  ]
}
